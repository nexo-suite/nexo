// Loads SvelteKit's ambient module declarations ($app/navigation, etc.)
// so shared components in @nexo/ui can import from $app/* and type-check
// without needing a SvelteKit project context.
import '@sveltejs/kit';
