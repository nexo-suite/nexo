## What

<!-- One or two sentences. What changed and why. -->

## Checklist

- [ ] `pnpm qc` passes locally
- [ ] No secrets or env values committed
- [ ] Dockerfile changes tested with `docker compose up --build`
- [ ] DB schema changes have a migration
